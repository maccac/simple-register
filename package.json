{
  "name": "berzerko-sale-calculator",
  "version": "1.0.0",
  "description": "Point-of-sale calculator for Berzerko. Static HTML/CSS/JS — no build step; npm is only used for tests.",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "scripts": {
    "check": "node --check public/data.js && node --check public/app.js",
    "test": "npm run check && node --test \"test/**/*.test.js\"",
    "serve": "node scripts/serve.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
